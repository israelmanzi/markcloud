services:
  markcloud:
    build: .
    env_file: .env
    environment:
      - DB_PATH=/data/markcloud.db
      - TEMPLATES_DIR=/app/templates
      - ADDR=:8080
    volumes:
      - markcloud-data:/data
    ports:
      - "8080:8080"
    restart: unless-stopped

volumes:
  markcloud-data:
